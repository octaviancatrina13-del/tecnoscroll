{
  "name": "tecnoscroll",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TecnoScroll — noticias de tecnología en español (feed continuo, contenido reescrito de forma original)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pipeline": "node scripts/run-pipeline.js",
    "pipeline:dry": "node scripts/run-pipeline.js --dry-run",
    "fetch": "node scripts/fetch-news.js"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.7",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
